import { Runner } from "../runner-context";
import { GenericRunnerOptions } from "./common";
export declare function INTEGER(options?: GenericRunnerOptions): Runner;
export declare namespace INTEGER {
    var wrapper: boolean;
}
