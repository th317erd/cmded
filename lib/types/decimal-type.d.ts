import { Runner } from "../runner-context";
import { GenericRunnerOptions } from "./common";
export declare function DECIMAL(options?: GenericRunnerOptions): Runner;
export declare namespace DECIMAL {
    var wrapper: boolean;
}
