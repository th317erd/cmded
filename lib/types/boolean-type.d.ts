import { Runner } from "../runner-context";
import { GenericRunnerOptions } from "./common";
export declare function BOOLEAN(options?: GenericRunnerOptions): Runner;
export declare namespace BOOLEAN {
    var wrapper: boolean;
    var parserOptions: {
        solo: boolean;
    };
}
